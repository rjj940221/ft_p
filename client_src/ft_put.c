//
// Created by rojones on 2017/07/06.
//

# include "ft_p_client.h"

void ft_put(char **argv)
{
	/*int fd;
	char *data;
	struct stat stat;

	if ((fd = open(argv[1], O_RDONLY)) == -1)
		return;
	if (fstat(fd, &stat) == -1)
		return;
	if ((data = mmap(0, (size_t) stat.st_size, PROT_READ, MAP_FILE | MAP_SHARED, fd, 0)) == MAP_FAILED)
		return;
	send(g_clt_env.svr_cmd_sock, line, strlen(line), 0);
	send(g_clt_env.svr_cmd_sock, data, (size_t) stat.st_size, 0);*/
}